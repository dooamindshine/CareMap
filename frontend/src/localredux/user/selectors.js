

export const SelectUserData = (state) => state.user.user;
export const SelectUserAddresses = (state) => state.user.addresses;
export const SelectLocations = (state) => state.user.locations;
export const SelectFacilities = (state) => state.user.facilities;
